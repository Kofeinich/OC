# !/bin/bash
date=$(date '+%F|%T')
mkdir "$HOME/test" 2>/dev/null && { echo "catalog test was created succesfully" > $HOME/report.txt; touch $HOME/test/$date; };
ping www.net_nikogo.www 2>/dev/null || echo "$(date +'%F_%H-%M-%S') Dont allow" >> $HOME/report
